import socket

HOST = "127.0.0.1"  # Loopback (localhost)
PORT = 7777         # Must match Unreal’s port

with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
    s.connect((HOST, PORT))
    s.sendall(b"Hello from Python!\n")
    s.close()


python tcp_test_client.py